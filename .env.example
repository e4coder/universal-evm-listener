# Alchemy API Key (used by Rust listener)
# Get your API key from https://dashboard.alchemy.com/
ALCHEMY_API_KEY=your_api_key_here

# SQLite Database Path (shared between Rust listener and Node.js API)
SQLITE_PATH=/home/ubuntu/universal_listener/data/transfers.db

# TTL in seconds (used by Rust listener for cleanup)
# Default: 600 (10 minutes). Can increase to 86400 (24 hours) for longer retention.
TTL_SECS=600

# API Server Port
API_PORT=5459

# Log level for Rust listener (trace, debug, info, warn, error)
LOG_LEVEL=info
